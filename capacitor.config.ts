import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.destinovacaciones',
  appName: 'destinovacaciones',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
